'use client'

// STUB — implemented by workflow agent "audio".
// Owns: procedural WebAudio SFX (no asset files): gunshot, reload, bat swing/charge,
// molotov, explosions, laser zaps, shield clank, robot deaths, boss beams, UI clicks,
// plus a light ambient bed. Subscribes to the events bus; AudioContext resumes on
// first user gesture. Renders nothing.
export function AudioSystem() {
  return null
}
