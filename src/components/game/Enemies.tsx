'use client'

// STUB — implemented by workflow agent "enemies".
// Owns: draining world.pendingSpawns, all 4 enemy AIs (melee/ranger/tank/sniper),
// articulated robot models with surface depth, hit flash, deaths (world.removeEnemy),
// tank bash telegraphs, sniper beam telegraphs + fire. Runs at FRAME_PRIO.enemies.
export function Enemies() {
  return null
}
