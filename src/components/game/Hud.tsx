'use client'

// STUB — implemented by workflow agent "hud".
// Owns: the DOM overlay (rendered OUTSIDE the Canvas): start menu, crosshair +
// bat charge ring, HP bar, ammo/molotov counters, weapon selector, wave banner,
// buff select cards, boss health bar, JUMP! warning, dodge cooldown, damage
// vignette, death/victory screens. Built from shadcn/ui components.
export function Hud() {
  return null
}
