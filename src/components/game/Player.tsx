'use client'

// STUB — implemented by workflow agent "player".
// Owns: pointer lock + mouse look, WASD, jump, dodge (i-frames, cooldown), gravity,
// capsule collision via world.resolveCapsule, camera placement at eye height,
// keeps world.player in sync every frame (FRAME_PRIO.player).
export function Player() {
  return null
}
