'use client'

// STUB — implemented by workflow agent "director".
// Owns: phase machine (wave 1..5 → buffSelect → smash → boss → victory/dead),
// wave composition (WAVES), drip-feeding world.dropRequests, wave-clear detection,
// buff offers (3 random), ammo crate spawn/render/pickup, run restarts (world.reset()).
// Runs at FRAME_PRIO.director.
export function Director() {
  return null
}
