'use client'

// STUB — implemented by workflow agent "vfx".
// Owns: red telegraph decals (circle/rect, pulse shaders), fire patch + beam wall
// rendering (custom shaders), explosions, muzzle flash, hit sparks, enemy death
// bursts, pickup glints — driven by world.telegraphs/world.hazards + events bus.
export function Vfx() {
  return null
}
