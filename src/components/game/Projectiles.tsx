'use client'

// STUB — implemented by workflow agent "projectiles".
// Owns: simulation + instanced rendering of world.projectiles (rangerBolt/bossBolt/
// molotov/rocket), obstacle + player + (for molotov) enemy collisions, central
// telegraph resolution at tHit (damage / explosion / beam spawn / jump-dodge rules),
// hazard damage-over-time (fire patches, beam walls). FRAME_PRIO.projectiles/hazards.
export function Projectiles() {
  return null
}
