'use client'

// STUB — implemented by workflow agent "weapons".
// Owns: weapon slots 1/2/3, view models (procedural pistol/bat/molotov meshes),
// pistol hitscan + reload + ammo, bat swing/charge (flash at max, 3x), molotov
// aim arc + landing disc + throw (world.addProjectile kind 'molotov').
// Runs at FRAME_PRIO.weapons.
export function Weapons() {
  return null
}
