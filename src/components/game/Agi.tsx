'use client'

// STUB — implemented by workflow agent "boss".
// Owns: the AGI — monitor head w/ pixel face canvas (happy/angry/hurt/tired/surprised),
// body + two telescoping arms, wave-phase hand drops (consumes world.dropRequests →
// world.spawnEnemy on release), floor smash sequence, all 5 boss attack patterns,
// tired/vulnerable windows (world.agi), death. Runs at FRAME_PRIO.boss.
export function Agi() {
  return null
}
